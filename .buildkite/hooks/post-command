if [[ "$BUILDKITE_JOB_ID" == "$BUILDKITE_BUILD_ID" ]]; then
  echo "Running post-command script"

  # Add your commands or script here
echo "This is an example of a post-command hook from .buildkite/hooks/post-command"
curl localhost:3000

  echo "Post-command script finished"
fi
